'use strict';

const {
    isCompleteBinaryTree
} = require('./is-complete');

const {
    isFullBinaryTree,
    isStrictBinaryTree
} = require('./is-full');

const {
    isPerfectBinaryTree
} = require('./is-perfect');

const {
    isHeightBalanced
} = require('./is-height-balanced');